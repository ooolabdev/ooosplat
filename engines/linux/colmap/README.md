# COLMAP runtime assets

`npm run setup:engines` installs the hash-locked COLMAP loop-closure
vocabulary tree here. The Ubuntu package embeds it so reconstruction never
downloads the asset at runtime.
